import { View, Text } from 'react-native'
import React from 'react'
import { Picker } from '@react-native-picker/picker'

export default function Picker(title) {
  return (
    <Picker>
        <Picker.item label/>
    </Picker>
  )
}